import * as React from 'react'

const PageTitle: React.FC = ({ children }) => (
  <h1 className="text-3xl font-title text-darkest md:text-5xl font-medium text-center">{children}</h1>
)

export default PageTitle
